# DeepStream + Triton playground

A playground for investigating and simplifying work with NVIDIA DeepStream 9.0
and Triton Inference Server — building tools and utilities that make
DeepStream much more user-friendly.

## Run it

```bash
./docker/launch.sh -b      # build the dev image
./docker/launch.sh -d      # drop into a dev shell with everything mounted
```
