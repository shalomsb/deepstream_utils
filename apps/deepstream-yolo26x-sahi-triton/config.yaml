source: /streams/sample_720p.h264

streammux:
  width: 1920
  height: 1080
  batch_size: 1

pgie:
  config_file: config_pgie_sahi.txt
  # Inference (ensemble input) resolution. SAHI tile geometry is baked
  # into sahi_preprocess.plan, so do NOT change these without rebuilding it.
  inference_width: 1280
  inference_height: 720
  labels_file: /models/yolo26/labels.txt

tracker:
  config_file: config_tracker.txt
